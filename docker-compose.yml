version: '3.8'

services:
  nuxt:
    image: node:18
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - '3000:3000'
    command: sh -c "npm install && npm run dev"
    environment:
      - HOST=0.0.0.0
      - PORT=3000
